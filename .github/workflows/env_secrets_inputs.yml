name: Demo CI Env Secrets

on:
  push:
  # workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Say Hello
        run: echo Hello, world from Second CI!

      - name: Install
        env:
          App_ENV: demo_new
          DB_PASSWORD : ${{secrets.DB_PASSWORD}}
        run: echo Environment ${App_ENV} using $DB_PASSWORD password
